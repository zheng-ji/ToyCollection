学习leveldb的内存设计思想

频繁的申请内存，不连续，会造成内存碎片。
Leveldb采用了内存池的概念，一次性申请4M的内存 如果下次读取的内存小于内存池的容量，直接获取，

否则，重新申请内存块：
    1.如果申请的字节数，bytes >= 1M，则申请内存为 bytes的块,避免内存块浪费
    2.如果申请的字节数，bytes <= 1M，则申请内存为 4M 的块


阅读leveldb/util/arena.h,实现的level_memo

===============================================
2013-05-29

